# 🏛️ الوثيقة المرجعية الشاملة لمعمارية قاعدة بيانات نظام إدارة الأسطول (Enterprise Fleet Management Platform Schema)

تُمثل هذه الوثيقة المرجع الهندسي المتكامل والنهائي للبنية التحتية لقواعد البيانات العلاقية الخاص بنظام إدارة الأسطول (Bin Zomah Fleet AI Enterprise Platform)، تم بناء وتصميم كافة الجداول، العلاقات، القيود البرمجية، والمشغلات الآلية (Triggers) بالاعتماد المباشر والكامل على البيانات التشغيلية لنظامك المُطور بـ Python / Flask / PostgreSQL / SQLAlchemy مع المحافظة على دقة الحقول بالهللة وتتبع السجلات بالثانية.

## 1. 🏢 النواة الإدارية والتنظيمية (Core Administrative Engine)
تُشكل هذه النواة الأساس الهيكلي للنظام، حيث تضمن العزل التام للبيانات بناءً على صلاحيات الفروع والمستخدمين مع تتبع كامل للحركات الأمنية.

```
┌───────────────────────────────────────┐
│     erp_branches (جدول الفروع)        │
└───────────────────┬───────────────────┘
                    │
                    ▼ (1:N)
┌───────────────────────────────────────┐
│    erp_users (جدول المستخدمين)        │
└───────────────────────────────────────┘
```

### 1.1 جدول الفروع التشغيلية (erp_branches)
- **المهمة التشغيلية:** إدارة النطاقات التنظيمية المتعددة للشركة وعزل عملياتها (مثل فرع الدمام).
- **البنية البرمجية والمفاتيح:**
  - `id (PK, SERIAL)`: المعرف الفريد للفرع.
  - `name (VARCHAR(100), NOT NULL)`: اسم الفرع التابع للشركة.
  - `code (VARCHAR(20), UNIQUE, NOT NULL)`: كود الفرع السري والمستنداتي (مثال: DMM-01).
- **قواعد التكامل (Integrity Constraints):** ترتبط بجميع الجداول التشغيلية كـ المفتاح الأجنبي الأساسي (branch_id) لضمان عدم تنفيذ أي معاملة بدون تحديد نطاق الفرع التابع لها.

### 1.2 جدول حسابات الصلاحيات والوصول (erp_users)
- **المهمة التشغيلية:** التحكم في وصول المستخدمين عبر نموذج الصلاحيات والأدوار (RBAC).
- **البنية البرمجية والمفاتيح:**
  - `id (PK, SERIAL)`: المعرف الفريد للنظام.
  - `branch_id (FK -> erp_branches.id)`: ربط الحساب بفرعه الرئيسي.
  - `username (VARCHAR(50), UNIQUE, NOT NULL)`: اسم المستخدم للدخول.
  - `password_hash (VARCHAR(255), NOT NULL)`: كلمة المرور المشفرة بخوارزميات غير قابلة للعكس (Argon2 / Bcrypt).
  - `role (ENUM('SUPER_ADMIN', 'FLEET_MANAGER', 'DATA_ENTRY', 'VIEWER'))`: الصلاحية المحددة في النظام.
  - `last_login (TIMESTAMP WITH TIME ZONE)`: توثيق تاريخ وساعة آخر دخول بالثواني.

## 2. 🚛 جداول الأسطول وحركة الأصول (Fleet & Asset Lifecycle)
ربط أداء المركبة بالسائق لحظياً لتتبع التاريخ التشغيلي والتسلسل الزمني لنقل المسؤولية والعُهد.

```
┌──────────────────────────┐          ┌──────────────────────────────────┐          ┌──────────────────────────┐
│  erp_vehicles (المركبة)  │ ◄──────► │ erp_vehicle_custody (سجل العهدة) │ ◄──────► │  erp_drivers (السائق)    │
└──────────────────────────┘          └──────────────────────────────────┘          └──────────────────────────┘
```

### 2.1 جدول بيانات السائقين والكوادر (erp_drivers)
- **المهمة التشغيلية:** تخزين كافة البيانات الوظيفية والشخصية والسجلات القانونية للسائقين والموظفين.
- **البنية البرمجية والمفاتيح:**
  - `id (PK, SERIAL)`: المعرف الفريد.
  - `branch_id (FK -> erp_branches.id)`: ربط السائق بالفرع التابع له.
  - `employee_id (VARCHAR(20), UNIQUE, NOT NULL)`: الرقم الوظيفي المولد تلقائياً.
  - `iqama_number (BIGINT, UNIQUE, NOT NULL)`: رقم الإقامة/الهوية الوطنية.
  - `phone (VARCHAR(20))`: رقم الجوال للتواصل والدخول لخدمات الذاتية.
  - `iqama_expiry` / `license_expiry (DATE, NOT NULL)`: تواريخ انتهاء الإقامة ورخصة القيادة.
  - `medical_exp` / `contract_exp (DATE)`: تواريخ انتهاء الفحص الطبي وعقد العمل (حقول متقدمة).
  - `status (ENUM('AVAILABLE', 'ON_TRIP', 'ON_LEAVE', 'TERMINATED'))`: حالة السائق التشغيلية.
  - `empNotes (TEXT)`: ملاحظات الموظف المخفية والمحددة للاستخدام الإداري السري.

### 2.2 جدول جرد المركبات والمعدات (erp_vehicles)
- **المهمة التشغيلية:** جرد وقيد كافة الشاحنات، السيارات الخاصة، والمعدات التابعة لأسطول الشركة.
- **البنية البرمجية والمفاتيح:**
  - `id (PK, SERIAL)`: المعرف الفريد للأصل.
  - `branch_id (FK -> erp_branches.id)`: ربط المركبة بالفرع.
  - `plate_number (VARCHAR(15), UNIQUE, NOT NULL)`: رقم اللوحة الرسمي.
  - `v_type (VARCHAR(50), NOT NULL)`: نوع المركبة (شاحنة ثقيلة، سيارة صغيرة، إلخ).
  - `serial_number (VARCHAR(50), UNIQUE, NOT NULL)`: الرقم التسلسلي للهيكل (VIN).
  - `gps_device_id (VARCHAR(50), INDEX)`: رقم جهاز التتبع المخفي للربط مع أنظمة GPS.
  - `current_km` / `odometer (INT, CHECK (current_km >= 0))`: القراءات الحالية لعداد الكيلومترات.
  - `fuel_card` / `opcard (VARCHAR(50))`: أرقام كروت الوقود وبطاقات التشغيل الصادرة.
  - `yard_condition` / `yard_status (VARCHAR(50))`: حالة الشاحنة داخل ساحة الوقوف أو خارجها على الطريق.

### 2.3 جدول عُهد المركبات (erp_vehicle_custody)
- **المهمة التشغيلية:** جدول مخفي تماماً يربط السائق بالمركبة لتحديد المسؤولية المالية والتشغيلية في أي لحظة زمنية.
- **البنية البرمجية والمفاتيح:**
  - `id (PK, SERIAL)`: المعرف الفريد لسجل العهدة.
  - `driver_id (FK -> erp_drivers.id)`: السائق المستلم.
  - `vehicle_id (FK -> erp_vehicles.id)`: المركبة المسلمة.
  - `received_date (TIMESTAMP WITH TIME ZONE, NOT NULL)`: تاريخ وساعة الاستلام الفعلي.
  - `returned_date (TIMESTAMP WITH TIME ZONE, NULLABLE)`: تاريخ التسليم والإخلاء.
  - `status (ENUM('ACTIVE', 'CLOSED'))`: حالة العهدة الحالية.
- **قواعد التكامل (Database Constraints):**
  - يُطبق قيد فريد جزئي لمنع إسناد السيارة لأكثر من سائق بحالة نشطة في نفس الوقت: `CREATE UNIQUE INDEX idx_active_custody ON erp_vehicle_custody (vehicle_id) WHERE status = 'ACTIVE';`

## 3. 🔧 الصيانة، المحروقات، والتحكم المالي (Maintenance, Inventory & Costing Schema)
صُمم هذا المحرك لإحكام الرقابة المالية والفنية على كافة مصاريف التشغيل والمخرجات المخزنية وتتبع الهدر بالهللة.

```
┌──────────────────────────────────────────────┐       ┌──────────────────────────────────────────┐
│  erp_workshop_records (أوامر صيانة الورشة)   │ ───►  │ erp_workshop_part_usage (استهلاك القطع)  │
└──────────────────────────────────────────────┘       └────────────────────┬─────────────────────┘
                                                                            │
                                                                            ▼
                                                       ┌──────────────────────────────────────────┐
                                                       │   erp_spare_parts (مستودع القطع)         │
                                                       └──────────────────────────────────────────┘
```

### 3.1 جدول سجلات الصيانة والورشة (erp_workshop_records)
- **المهمة التشغيلية:** تسجيل وإدارة أوامر الصيانة (فتح وإغلاق الكروت الفنية).
- **البنية البرمجية والمفاتيح:**
  - `id (PK, SERIAL)`: رقم أمر الصيانة الفريد.
  - `vehicle_id (FK -> erp_vehicles.id)`: المركبة التي أُجريت لها الصيانة.
  - `entry_date (TIMESTAMP, NOT NULL)`: تاريخ وساعة تسجيل دخول الورشة.
  - `close_date (TIMESTAMP, NULLABLE)`: تاريخ وساعة إغلاق أمر الصيانة.
  - `mechanic_name (VARCHAR(100))`: اسم الميكانيكي المسؤول عن الإصلاح.
  - `issue_description (TEXT)`: الوصف الفني المفصل للعطل أو الصيانة الدوريّة.

### 3.2 جدول مستودع قطع الغيار (erp_spare_parts)
- **المهمة التشغيلية:** إدارة المخزون المالي والفني للقطع المشتراة وتتبع أداء الموردين.
- **البنية البرمجية والمفاتيح:**
  - `id (PK, SERIAL)`: المعرف المباشر للقطعة.
  - `part_number (VARCHAR(50), UNIQUE, NOT NULL)`: الرقم المرجعي للقطعة المصنعة.
  - `quantity (INT, CHECK (quantity >= 0))`: الكمية الفعلية المتوفرة بالمخزن.
  - `price (DECIMAL(10, 2), NOT NULL)`: التكلفة المالية بسعر الشراء المباشر.
  - `supplier (VARCHAR(100))`: اسم المورد الأساسي المعتمد للقطعة.

### 3.3 جدول تفاصيل استهلاك القطع (erp_workshop_part_usage)
- **المهمة التشغيلية:** ربط القطعة المسحوبة بأمر الصيانة مباشرة للحد من التسريب أو السرقة والهدر المالي.
- **البنية البرمجية والمفاتيح:**
  - `id (PK, SERIAL)`: المعرف الفريد للعملية.
  - `work_order_id (FK -> erp_workshop_records.id)`: رقم أمر الصيانة المرتبط.
  - `part_id (FK -> erp_spare_parts.id)`: القطعة المسحوبة.
  - `quantity_used (INT, CHECK (quantity_used > 0))`: الكمية المصروفة فعلياً.
  - `cost_at_time (DECIMAL(10, 2))`: تثبيت سعر القطعة وقت الاستهلاك بدقة لضمان صحة الميزانيات المالية وعدم التأثر بتغير الأسعار لاحقاً.

### 3.4 جدول تموين المحروقات والدعم المالي (erp_fuel_records)
- **المهمة التشغيلية:** رصد مصروفات الديزل والبنزين واحتساب كفاءة التموين بالهللة.
- **البنية البرمجية والمفاتيح:**
  - `id (PK, SERIAL)`: سجل عملية التزويد.
  - `vehicle_id (FK -> erp_vehicles.id)`: الشاحنة أو المركبة المستفيدة.
  - `driver_id (FK -> erp_drivers.id)`: السائق الذي قام بالتعبئة.
  - `prev_odo (INT, NOT NULL)`: قراءة العداد السابقة عند آخر تعبئة.
  - `current_odo (INT, NOT NULL, CHECK (current_odo > prev_odo))`: قراءة العداد الحالية لحظة التزويد.
  - `liters (DECIMAL(8, 2), NOT NULL)`: كمية الوقود المعبأة باللترات.
  - `cost (DECIMAL(10, 2), NOT NULL)`: التكلفة المالية المباشرة المخصومة بالهللة.

## 4. 🚨 جداول التوثيق والرقابة الأمنية (Governance, Audit & Logistics Schema)
تضمن هذه الجداول الامتثال الكامل، الرقابة القانونية، والحفاظ على الملفات المرفقة والتعديلات الرقمية بدون أي إمكانية للتلاعب.

### 4.1 سجل التدقيق البرمجي الشامل (erp_audit_logs)
- **المهمة التشغيلية:** (جدول مخفي 100%) يُسجل كل حركة أو تعديل أو حذف يحدث داخل النظام لتوفير تتبع أمني حاد (Digital Forensics).
- **البنية البرمجية والمفاتيح:**
  - `id (BIGINT, PK, SERIAL)`: المعرف التتابعي المفرّد.
  - `user_id (FK -> erp_users.id)`: تحديد المستخدم المنفذ للإجراء.
  - `action (VARCHAR(50), NOT NULL)`: نوع الحركة (INSERT, UPDATE, DELETE).
  - `timestamp (TIMESTAMP WITH TIME ZONE, DEFAULT CURRENT_TIMESTAMP)`: توثيق الوقت بالثانية والدقيقة.
  - `reason (TEXT)`: المبرر المدخل إجبارياً في حال وجود تعديل حساس أو حذف لسجل مالي/تشغيلي.
  - **سياسة الحماية:** تُسحب صلاحيات التعديل والحذف (UPDATE / DELETE) نهائياً عن هذا الجدول لتكون السجلات قائمة على مبدأ الإضافة فقط (Append-Only).

### 4.2 جدول السلامة والحوادث (erp_incidents)
- **المهمة التشغيلية:** تسجيل المخالفات المرورية، الحوادث، والأعطال الطارئة وتحديد تبعاتها المالية.
- **البنية البرمجية والمفاتيح:**
  - `id (PK, SERIAL)`: المعرف الفريد للسجل.
  - `vehicle_id (FK -> erp_vehicles.id)`: المركبة المتأثرة بالحادث.
  - `driver_id (FK -> erp_drivers.id)`: السائق المتواجد أثناء الحادث/المخالفة.
  - `type (VARCHAR(50), NOT NULL)`: تصنيف الحدث (حادث مروري، مخالفة سرعة، عطل طارئ).
  - `amount (DECIMAL(10, 2), DEFAULT 0.00)`: التكلفة التقديرية أو الغرامة المالية المترتبة.
  - `location (VARCHAR(255))`: موقع وقوع الحادث أو المخالفة.
  - `description (TEXT)`: الوصف السري والمفصل للحادث لتقدير المسؤوليات الإدارية.

### 4.3 أرشيف المستندات والوثائق الرقمية (erp_documents)
- **المهمة التشغيلية:** حفظ النسخ الرقمية (الصور، مستندات الـ PDF) وإلغاء الحاجة للملفات الورقية نهائياً.
- **البنية البرمجية والمفاتيح:**
  - `id (PK, SERIAL)`: الرقم المرجعي للملف.
  - `file_path (VARCHAR(255), NOT NULL)`: المسار الفعلي المشفر للملف على السيرفر.
  - `file_size (INT, NOT NULL)`: حجم الملف بالبايت.
  - `mime_type (VARCHAR(50), NOT NULL)`: نوع صيغة الملف (application/pdf, image/jpeg).

---

## 📊 ملخص الضوابط والمعايير المطبقة داخل قاعدة البيانات

| المعيار الهندسي والتشغيلي | الآلية المطبقة بداخل Schema قاعدة البيانات | الفائدة الفنية والمالية للنظام |
|---|---|---|
| **Referential Integrity** | ربط كافة الجداول بمفاتيح أجنبية FOREIGN KEY مع سياسات ON DELETE RESTRICT. | حماية البيانات من الحذف العشوائي وتلف الشجرة العلاقية بين الجداول. |
| **Data Normalization** | تطبيق النموذج المعياري الثالث (3NF) لتقليل التكرار وتوزيع الجداول. | رفع سرعة الاستعلامات ومنع تضخم قاعدة البيانات بسرعة. |
| **Financial Accuracy** | الاعتماد الحصري على نوع البيانات DECIMAL(10,2) للمحروقات والقطع. | القضاء التام على أخطاء التقريب بالهللة في القوائم والميزانيات المالية. |
| **Odometer Validation** | فرض شرط CHECK (current_odo > prev_odo) في جدول الوقود. | حظر إدخال أي قراءات عداد عكسية أو خاطئة من قبل مدخلي البيانات. |
| **Audit Traceability** | أتمتة الإدراج بجدول erp_audit_logs لكل عملية إضافة أو حذف. | تتبع كامل لعمليات الموظفين ومنع التلاعب بالسجلات الإدارية. |
