# خطة التشغيل والحماية

## ما تم تفعيله

أضيفت صفحة **جودة البيانات** (`/data-quality`) لفحص السائقين والمركبات والعهد النشطة. الفحص قراءة فقط، ويعرض الحقول الناقصة والروابط غير المكتملة دون تعديل البيانات.

أضيفت صفحة **سجل التدقيق** (`/audit-log`) لعرض العمليات المسجلة والبحث فيها. لا تحتوي الصفحة على وظيفة حذف أو تعديل.

تم تشديد عملية حفظ ربط السائق بالمركبة لتتطلب دور `admin` أو `operations`، مع تسجيل العملية في سجل التدقيق.

## النسخ الاحتياطي

الأداة `tools/backup_database.py` تنشئ نسخة مؤرخة من قاعدة البيانات. لا تنفذ الأداة أي حذف أو تحديث للبيانات التشغيلية.

يجب ضبط المتغيرات التالية في بيئة الاستضافة:

```text
DATABASE_URL=postgresql://...
BACKUP_DIR=/secure/backups
```

للتشغيل اليدوي:

```bash
python3 tools/backup_database.py
```

يمكن جدولة التشغيل يوميًا من لوحة الاستضافة أو Cron في وقت منخفض الاستخدام. يجب حفظ النسخ في مساحة منفصلة ومحمية، مع اختبار الاستعادة على نسخة غير إنتاجية قبل اعتمادها.

## الاستعادة

لا تُنفذ الاستعادة فوق قاعدة الإنتاج مباشرة. الخطوات الآمنة هي إنشاء قاعدة اختبار، استعادة النسخة إليها، فحص الجداول وعدد السجلات، ثم اعتماد خطة التحويل بعد موافقة الإدارة.

## الإرسال الخارجي

ما زال إرسال البريد وواتساب منفصلًا عن فحص جودة البيانات. يجب إبقاء وضع المحاكاة حتى اعتماد المستلمين والقنوات، ثم اختبار قناة واحدة قبل التفعيل الكامل.
