-r requirements.txt

# Test-only dependency: tests/test_plate_normalization.py uses pytest.mark.parametrize.
# Not installed in production (Dockerfile/start.sh only reference requirements.txt).
pytest==8.3.3
